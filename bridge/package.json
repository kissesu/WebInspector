{
  "name": "web-inspector-bridge",
  "version": "1.0.0",
  "description": "WebSocket bridge for Web Inspector → Claude Code CLI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:clipboard": "node server.js --clipboard"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.1",
    "ws": "^8.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
